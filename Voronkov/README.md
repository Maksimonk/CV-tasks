# Сравнение ResNet50 и MobileNetV3 на датасете Cats vs Dogs

Этот проект посвящен решению задачи бинарной классификации изображений с использованием методов переносного обучения (Transfer Learning). В работе сравниваются две популярные архитектуры нейронных сетей в связке с библиотекой TensorFlow/Keras.

## 📌 Описание проекта
Основная цель — построить модель, способную классифицировать изображения кошек и собак, используя предобученные на ImageNet веса. Особое внимание уделено этапу **очистки данных**, так как исходный набор данных содержит битые файлы и несовместимые форматы.

## 🛠 Технологический стек
* **Язык:** Python 3.12
* **Библиотеки:** TensorFlow, Keras, PIL (Pillow), Matplotlib, NumPy
* **Среда:** Google Colab

## 📂 Данные
Используется датасет [Microsoft Cats vs Dogs](https://www.kaggle.com/datasets/shaunthesheep/microsoft-catsvsdogs-dataset).
- **Количество классов:** 2 (Кошки и Собаки).
- **Особенности:** Датасет содержит "битые" изображения (0 байт) и файлы в нестандартных форматах (например, 2-канальные изображения), что требует предварительной обработки.

## 🧹 Предобработка и очистка
Для обеспечения стабильного обучения реализован многоступенчатый процесс очистки:
1. **Физическая очистка:** Удаление файлов, не являющихся изображениями или имеющих некорректные заголовки.
2. **Конвертация в RGB:** Принудительное приведение всех картинок к 3-канальному формату RGB для исключения ошибок `InvalidArgumentError`.
3. **Отказоустойчивость:** Использование `tf.data.experimental.ignore_errors()` при создании датасета, чтобы пропускать поврежденные файлы "на лету" во время обучения.

## 🏗 Архитектура моделей
В проекте реализована функция `build_model`, которая позволяет легко подключать различные базовые сети:

```python
def build_model(base_model, preprocess_fn):
    base_model.trainable = False
    inputs = layers.Input(shape=(224,224,3))
    x = preprocess_fn(inputs)
    x = base_model(x, training=False)
    x = layers.GlobalAveragePooling2D()(x)
    x = layers.Dropout(0.3)(x)
    outputs = layers.Dense(1, activation='sigmoid')(x)
    
    model = models.Model(inputs, outputs)
    model.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])
    return model